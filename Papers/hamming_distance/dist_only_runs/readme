these runs were done over 40 connectivities ranging from 0.01 to 0.20 with 10 samples per connectivity. there is no saturation data in these runs

n_neurons = 100
n_synapses = int((n_neurons ** 2) * connectivity)

sim_time = 100

min_f = 1
max_f = 100
sim_f = 100

n_enc = 5
